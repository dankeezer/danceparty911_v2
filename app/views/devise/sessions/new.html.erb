<style type="text/css">

.margin{
  margin-bottom: 10px;
}

.pink_h2
{
  color: #FF33CC !important;
  letter-spacing:-1px;
}

</style>


<div class="account-wall center">
  <center><h2 class="pink_h2">sign in</h2></center>
  <%= simple_form_for(resource, :class => "form-signin center", :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <% flash.each do |name, msg| %>
      <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
        <a class="close" data-dismiss="alert">×</a>
        <%= msg %>
      </div>
    <% end %>
    <div class="margin"><%= f.email_field :email, :class => "form-control", :placeholder => "Email", :autofocus => true %></div>
    <div class="margin"><%= f.password_field :password, :class => "form-control", :placeholder => "Password" %></div>
    <% if devise_mapping.rememberable? -%>
      <div class="checkbox"><%= f.check_box :remember_me  %> <%= f.label :remember_me %></p></div>
    <% end -%>
    <%= f.submit "Sign in", :class => "btn btn-sm" %>
  <% end %>
</div>