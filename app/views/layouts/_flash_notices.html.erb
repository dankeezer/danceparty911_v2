<div class="noticesWrapper">
  <% flash.each do |msg| %>
    <div class="alert alert-<%= msg.first %>">
      <a class="close" data-dismiss="alert">×</a>
      <%= msg.last %>
    </div>
  <% end %>
</div>