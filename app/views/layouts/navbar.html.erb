<!DOCTYPE html>
<html>
<head>
  <title>danceparty911</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

<style type="text/css">

input[type="email"],
input[type="password"],
.uneditable-input {
	margin-bottom: 0px !important;
}

.btn {
  font-family: Helvetica, sans-serif;
  background-color: #FFFFFF;
  *background-color: #FFFFFF;
  border-color: #FFFFFF #FFFFFF #FFFFFF;
  color: #666;
  letter-spacing: .5px;
  background-image: -moz-linear-gradient(top, #ffffff, #ffffff);
  background-image: -webkit-gradient(linear, 0 0, 0 100%, from(#ffffff), to(#ffffff));
  background-image: -webkit-linear-gradient(top, #ffffff, #ffffff);
  background-image: -o-linear-gradient(top, #ffffff, #ffffff);
  background-image: linear-gradient(to bottom, #ffffff, #ffffff);
}

.btn-sm {
	margin: 0px !important;
	font-weight: normal;
	background-color: #FFFFFF;
}

.navbar {
	margin: 0px;
	padding: 3px;
	height: 31px;
	background-color: #FF33cc;
	/*border-bottom:solid;
	border-width:2px;
	border-color: #555555;*/
}

.navbar-brand {
	padding: 3px;
}

.navbar-link {
  color: #FFFFFF !important;
  padding: 3px;
}

.navbar-text {
  line-height: 0px !important;
}

.myNav {
}

.soundcloud_image {
	border-width:1px;
	border-color: #FF33cc;
	padding-bottom: 1px;
}

</style>

</head>

<body>

	<div class="myNav">
		<div class="navbar">
		<% if user_signed_in? %>
			<div class="form-group">
				<%= link_to image_tag "911logo548.gif", size: "175", :class => "navbar-brand" %>
				<% if current_user.username.nil? %>
					<p class="navbar-text pull-right"><%= link_to current_user.email, edit_user_registration_path, :class => "navbar-link" %>
				<% else %>
					<p class="navbar-text pull-right"><%= link_to current_user.username, edit_user_registration_path, :class => "navbar-link" %>
				<% end %>
				<%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => "btn btn-sm" %></p>	
			</div>
		<% else %>
			<%= simple_form_for(resource, :class => "form-signin", :as => resource_name, :url => session_path(resource_name)) do |f| %>
			<div class="form-group">
				<%= link_to image_tag "911logo548.gif", size: "175", :class => "navbar-brand" %>
				<%= f.email_field :email, :class => "form-control", :placeholder => "Email", :autofocus => true %>
				<%= f.password_field :password, :class => "form-control", :placeholder => "Password" %>

				<%= f.submit "Sign in", :class => "btn btn-sm" %>
				<%= link_to image_tag("btn-connect-sc-l.png"), user_omniauth_authorize_path(:soundcloud), :class => "soundcloud_image" %>
				<%= link_to "Sign up", new_user_registration_path, :class => "btn btn-sm pull-right" %>
			</div>
			<% end %>
		<% end %>
	</div>
	
	<% flash.each do |name, msg| %>
	  <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
	    <a class="close" data-dismiss="alert">×</a>
	    <%= msg %>
	  </div>
	<% end %>


	<%= yield %>
</body>

</html>
